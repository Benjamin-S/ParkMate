@model QueryResult<Customer>;

@{
    ViewData["Title"] = "My Bookings";
}

<div>
    <partial name="_UserProfilePartial"/>
</div>

<h2>My Bookings</h2>

<div>    
    @{
        @*
        if (Model.Success != true)
        {
            <div class="alert alert-danger" role="alert">
                @Model.Message
            </div>
        }
        else if (Model.PayLoad.Bookings.FutureBookings?.Any() != true)
        {
            <div class="alert alert-info" role="alert">
                It appears you currently have no future bookings planned.
            </div>
        }
        else
        {
            foreach (var booking in Model.PayLoad.Bookings.FutureBookings)
            {
                <p>@booking.BookingTime | @booking.ParkingSpace.Description.Title</p>
            }
        }     
        *@
    }
</div>

@section Scripts
{
    <script>
        $("#myBookings").addClass("active");
    </script>
}